{
  "tasks": {
    "build": "deno run -A build/esbuild-build.ts",
    "serve": "deno run --allow-read --allow-net --allow-env serve.ts"
  },
  "deploy": {
    "org": "ubiquity-dao",
    "app": "p-onboard-ubq-fi"
  }
}
